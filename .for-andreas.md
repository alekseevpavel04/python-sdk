# Status Update

## Completed Work

### 1. API Integration and Testing

Successfully adapted the Python SDK to the latest platform API changes deployed on dev:

- **Code Generation**: Pulled `openapi.json` from dev, archived as `in/archive/openapi_1.0.0-beta.3.json`, and regenerated client code
- **Module Updates**: Updated `platform` and `application` modules to align with generated code changes
- **Test Coverage**: All tests in the `platform` module pass locally against dev (verified via VSCode test runner)
- **Partial Validation**: E2E tests in the `application` module cannot complete due to missing application version registrations on dev
- **Feature Integration**: Implemented new API capabilities (statistics, granular status codes) across service, CLI, and GUI layers
- **Quality Assurance**: Added `pyright` as an additional linter (integrated with `make lint`) to mitigate risk from untested code paths

**Critical Issues Identified**:

- Multiple API/OpenAPI specification discrepancies discovered - see TODOs tagged with your name in `codegen/` and `src/` directories
- **Blocker**: API returns `None` for application versions while OpenAPI spec disallows null values. Applied temporary workaround by modifying generated code. **Do not regenerate code before resolving these TODOs.**

### 2. CI/CD Environment Configuration

Prepared infrastructure for staging environment integration:

- Refactored reusable GitHub workflows (prefix `_`) to accept `platform_environment` input parameter (see `.github/workflows/_test.yml#L6` and `.github/workflows/ci-cd.yml#L38`)
- Configured "staging" as default platform environment for test execution
- Added scheduled test suite for staging environment (in addition to existing production schedule)

### 3. Monitoring Infrastructure

Configured Betterstack monitoring for staging environment (currently paused, ready for activation):

- Created heartbeat monitor for scheduled staging tests
- Created uptime monitor for staging API endpoint
- Added monitors to public status page

## Next Steps (Recommended Sequence)

### 1. Local Environment Setup

Pull this branch and create `~/.aignostics/.env` with the following configuration:

```shell
API_ROOT=https://platform-dev.aignostics.ai
AIGNOSTICS_LOG_LEVEL=DEBUG
AIGNOSTICS_LOG_FILE_ENABLED=true
AIGNOSTICS_LOG_FILE_NAME=/tmp/aignostics.log
# AIGNOSTICS_LOG_CONSOLE_ENABLED=true
GOOGLE_APPLICATION_CREDENTIALS=/Users/andreas/.aignostics/aignx-platform-api-sa.json
```

### 2. Verify Dev Environment Connectivity

Execute `uv run aignostics application list` to confirm dev environment communication. Expected output (note `None` values due to missing application versions):

```text
Available Aignostics Applications:
- he-tme - latest application version: `None`
- test-app - latest application version: `None`
```

### 3. Deploy Application Versions

Ensure both `heta` and `test` applications have registered versions on dev and staging environments.

### 4. Validate Deployment

Re-run `uv run aignostics application list` and verify application versions are displayed instead of `None`.

### 5. Configure GitHub Secrets

Navigate to [GitHub Actions Secrets](https://github.com/aignostics/python-sdk/settings/secrets/actions) and configure the following environment-specific secrets:

- `AIGNOSTICS_REFRESH_TOKEN_PRODUCTION`
- `AIGNOSTICS_REFRESH_TOKEN_STAGING`
- `GCP_CREDENTIALS_PRODUCTION`
- `GCP_CREDENTIALS_STAGING`

Also configure the same secrets for [Dependabot](https://github.com/aignostics/python-sdk/settings/secrets/dependabot). All other environment-dependent secrets are already configured.

### 6. Run CI/CD Pipeline

Push changes to GitHub to execute full test suite against staging environment.

### 7. Local Test Execution

In parallel with Step 6, execute tests in the `application` module tagged with your name via:

```shell
uv run pytest -k <testname>
```

Or use the VSCode test runner. (Other tests already pass.)

### 8. API Specification Update

Coordinate with the team to:

1. Update `openapi.json` on dev environment
2. Assign proper version (recommend `1.0.0-beta.7`, assuming current is `1.0.0-beta.6`)
3. Execute `make codegen` to pull updated specification and archive it in `codegen/in/archive/`
4. Regenerate client code

### 9. Iterative Testing

Continue iteration until all tests pass on GitHub CI for at least the `ubuntu-latest` runner.

### 10. Enable Staging Monitors

Unpause Betterstack monitors:

- [Heartbeat Monitor for Applications](https://uptime.betterstack.com/team/t318777/heartbeats/433359)
- [API Uptime Monitor](https://uptime.betterstack.com/team/t318777/monitors)

### 11. Release Process

After monitors run successfully for 24 hours:

1. Create pull request
2. Request review from AI assistants (Claude/Copilot, both active on repository)
3. Merge when all checks pass
4. Execute `make bump` to publish new SDK release

## Additional Notes

1. **Parallel Development**: I may continue improving other SDK aspects (particularly documentation). Please rebase your changes on top of mine to avoid merge conflicts.
2. **Support Available**: Contact me if you encounter any blockers or have questions.
