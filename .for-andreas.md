# Status / Done

1. Adapted to latest API changes found on dev, tested as far as possible given no application version registered on dev. Tests of platform module pass, Some E2E tests in application module fail given
deployment incomplete on dev

- Pulled latest openapi.json found on dev, archived as in/archive/openapi_1.0.0-beta.3.json, and used for codegen
- Adapted platform and application modules to updated codegen
- Adapted tests of platform module to changes in platform and application modules -> all tests in platform module pass when run locally against dev, e.g. using VSCode test runner
- Adapted tests to application module -> E2E tests cannot pass, as no application version registered on dev atm
- Used new features provided by API such as statistics, more fine granular status, in service, cli and gui layer etc. However could not try out, given no application version registered on dev
- Added pyright as additional linter (integrated with make lint) to reduce probabilty of having introduced errors given i cannot try out significant parts of the code given no application version registered on dev
- Important note: Found a few issues in the API / openapi.json: Please check TODOs tagged with your name in codegen/ and src/ folder tagged in your name. In particular the API serves none for the application versions, while the openapi.json does not allow that. Had to workaround that by modifying the generated! code. Please do not regenerate the code before having checked the TODOs tagged with your name in the codegen/ folder at least.

2. Prepared that you can run the CI/CD against staging when made public. Very easy to complete.

- Made reuseable Github workflows (the ones starting with _in .github/workflows) react to platform_environmet input, see e.g. .github/workflows/_test.yml#l6, as well as .github/workflows/ci-cd.yml#l38
- Set "staging" as the platform environment for tests
- Added scheduled test running against staging, *in addition* to keeping the one running against production

3. Prepared that betterstack monitors staging in addition to production. 2 button clicks left to activate.

- Created heartbeat monitor for scheduled test on staging in betterstack, currently paused
- created regular monitor for API on staging, currently paused
- added to status page

## Todos / What I would do next (in this order)

1. Ensure both heta and test have application versions deployed on dev and staging.
2. Run `uv run aignostics application list` and check this now shows application versions, not `None` as a replacement
3. Goto <https://github.com/aignostics/python-sdk/settings/secrets/actions> and set the following 4 secrets,
   given I did not have the values handy: AIGNOSTICS_REFRESH_TOKEN_{PRODUCTION,STAGING}, GCP_CREDENTIALS_{PRODUCTION,STAGING},
   same for dependabot: <<https://github.com/aignostics/python-sdk/settings/secrets/dependabot> (same keys)
   The other prod/staging dependent ones are already set. Prepared the above ones for you, so you can do it faster
4. Push to GitHub to see if the tests in the applications module pass there
5. In parallel to 4: Run the tests in the application module tagged with your name (the other ones already pass). You can do so via `uv run pytest -k <testname>` or the VSCode test runner
6. Have the team update the openapi.json on dev, put in a proper version name (I guess 1.0.0-beta.7, given the
   latest one should be called 1.0.0-beta.6) and run `make codegen`. This will pull the openapi.json from
   dev and archive it according to the version found in the codegen/in/archive folder, then codegen is run.
7. Iterate until all tests pass on github at least for the ubuntu latest runner.
8. Unpause the heartbeat monitor for applications on staging on betterstack here (hit > Unpause): <https://uptime.betterstack.com/team/t318777/heartbeats/433359>
9. Unpause the  monitor for API on staging on betterstack here (hit > Unpause): <https://uptime.betterstack.com/team/t318777/monitors>
10. When they are both unpaused and applications are passing for a day, create PR, possibly have reviewed by Claude and CoPilot (both active on this repo), and merge when passing; run `make bump` to cut a release of python sdk

## Further notes

1. I am in parallel working on supporting NiceGUI 3 which contains breaking changes and was released Oct 4, i.e - branch name is feat/nicegui_3 - this is independent of the update to the Platform API. Just noting so you
are aware and not confused when you see activity on that branch
2. Please call in case you are stuck or have questions, happy to help, as always.
