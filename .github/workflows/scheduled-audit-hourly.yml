name: "+ Scheduled Audit (Hourly)"

on:
  schedule:
    - cron: '0 * * * *'

jobs:
  audit-scheduled:
    uses: ./.github/workflows/_scheduled-audit.yml
    permissions:
      contents: read
      id-token: write
    secrets:
      BETTERSTACK_AUDIT_HEARTBEAT_URL: ${{ secrets.BETTERSTACK_AUDIT_HEARTBEAT_URL }}
